return {
  buffer = require("utils.buffer"),
  edit = require("utils.edit"),
  fs = require("utils.fs"),
  highlights = require("utils.highlights"),
  lua = require("utils.lua"),
  math = require("utils.math"),
}
